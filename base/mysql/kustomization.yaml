apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app: mysql
  type: external

resources:
- external-service.yaml

secretGenerator:
- name: mysql-credentials
  literals:
  - username=wordpress
  - password=changeme
  - database=wordpress
  - host=mysql-external
